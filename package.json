{
  "name": "animpure-workspace",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "npm run build --workspace=@gozenc/animpure && npm run build --workspace=studio",
    "check": "npm run check --workspace=studio",
    "dev": "npm run dev --workspace=studio"
  }
}
